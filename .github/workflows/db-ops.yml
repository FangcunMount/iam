name: Database Operations

# Production host contract:
# - prefer pre-provisioned official MySQL 8.x clients.
# - if absent, use the official mysql:8.0 image through passwordless sudo Docker.
# - the workflow never installs host packages.
# - credentials are passed through a temporary 0600 defaults file by the script.

on:
  workflow_dispatch:
    inputs:
      operation:
        description: Database operation
        required: true
        type: choice
        options:
          - backup
          - restore
          - status
          - performance-schema-status
          - rolebinding-guard-preflight
          - rolebinding-deduplicate-dry-run
          - rolebinding-deduplicate-apply
      backup_name:
        description: Backup name, required for restore and RoleBinding apply
        required: false
        type: string
      candidate_fingerprint:
        description: Exact dry-run fingerprint, required for RoleBinding apply
        required: false
        type: string
      candidate_count:
        description: Exact dry-run candidate count, required for RoleBinding apply
        required: false
        type: string
  schedule:
    - cron: '0 17 * * *'  # Daily 17:00 UTC / 01:00 Asia-Shanghai

permissions:
  contents: read

jobs:
  db-backup:
    name: Database Backup
    if: github.event_name == 'schedule' || github.event.inputs.operation == 'backup'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Backup Database
        uses: appleboy/ssh-action@v1.2.5
        env:
          IAM_DB_OPS_OPERATION: backup
          IAM_DB_OPS_ALLOW_DOCKER_CLIENT: "1"
          MYSQL_HOST: ${{ secrets.MYSQL_HOST }}
          MYSQL_PORT: ${{ secrets.MYSQL_PORT || 3306 }}
          MYSQL_USERNAME: ${{ secrets.MYSQL_USERNAME }}
          MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
          MYSQL_DBNAME: ${{ secrets.MYSQL_DBNAME }}
        with:
          host: ${{ vars.SVRB_PUBLIC_HOST || secrets.SVRB_PUBLIC_HOST || vars.SVRB_HOST || vars.SVRA_HOST }}
          username: ${{ vars.SVRB_USERNAME || vars.SVRA_USERNAME }}
          key: ${{ secrets.SVRB_SSH_KEY || secrets.SVRA_SSH_KEY }}
          port: ${{ vars.SVRB_SSH_PORT || vars.SVRA_SSH_PORT || 22 }}
          envs: IAM_DB_OPS_OPERATION,IAM_DB_OPS_ALLOW_DOCKER_CLIENT,MYSQL_HOST,MYSQL_PORT,MYSQL_USERNAME,MYSQL_PASSWORD,MYSQL_DBNAME
          script_path: scripts/dbops/database-operation.sh

  db-restore:
    name: Database Restore
    if: github.event.inputs.operation == 'restore'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Restore Database
        uses: appleboy/ssh-action@v1.2.5
        env:
          IAM_DB_OPS_OPERATION: restore
          IAM_DB_OPS_ALLOW_DOCKER_CLIENT: "1"
          IAM_DB_OPS_BACKUP_NAME: ${{ github.event.inputs.backup_name }}
          MYSQL_HOST: ${{ secrets.MYSQL_HOST }}
          MYSQL_PORT: ${{ secrets.MYSQL_PORT || 3306 }}
          MYSQL_USERNAME: ${{ secrets.MYSQL_USERNAME }}
          MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
          MYSQL_DBNAME: ${{ secrets.MYSQL_DBNAME }}
        with:
          host: ${{ vars.SVRB_PUBLIC_HOST || secrets.SVRB_PUBLIC_HOST || vars.SVRB_HOST || vars.SVRA_HOST }}
          username: ${{ vars.SVRB_USERNAME || vars.SVRA_USERNAME }}
          key: ${{ secrets.SVRB_SSH_KEY || secrets.SVRA_SSH_KEY }}
          port: ${{ vars.SVRB_SSH_PORT || vars.SVRA_SSH_PORT || 22 }}
          envs: IAM_DB_OPS_OPERATION,IAM_DB_OPS_BACKUP_NAME,IAM_DB_OPS_ALLOW_DOCKER_CLIENT,MYSQL_HOST,MYSQL_PORT,MYSQL_USERNAME,MYSQL_PASSWORD,MYSQL_DBNAME
          script_path: scripts/dbops/database-operation.sh

  db-status:
    name: Database Status
    if: github.event.inputs.operation == 'status'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Check Database Status
        uses: appleboy/ssh-action@v1.2.5
        env:
          IAM_DB_OPS_OPERATION: status
          IAM_DB_OPS_ALLOW_DOCKER_CLIENT: "1"
          MYSQL_HOST: ${{ secrets.MYSQL_HOST }}
          MYSQL_PORT: ${{ secrets.MYSQL_PORT || 3306 }}
          MYSQL_USERNAME: ${{ secrets.MYSQL_USERNAME }}
          MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
          MYSQL_DBNAME: ${{ secrets.MYSQL_DBNAME }}
        with:
          host: ${{ vars.SVRB_PUBLIC_HOST || secrets.SVRB_PUBLIC_HOST || vars.SVRB_HOST || vars.SVRA_HOST }}
          username: ${{ vars.SVRB_USERNAME || vars.SVRA_USERNAME }}
          key: ${{ secrets.SVRB_SSH_KEY || secrets.SVRA_SSH_KEY }}
          port: ${{ vars.SVRB_SSH_PORT || vars.SVRA_SSH_PORT || 22 }}
          envs: IAM_DB_OPS_OPERATION,IAM_DB_OPS_ALLOW_DOCKER_CLIENT,MYSQL_HOST,MYSQL_PORT,MYSQL_USERNAME,MYSQL_PASSWORD,MYSQL_DBNAME
          script_path: scripts/dbops/database-operation.sh

  db-controlled-operation:
    name: Controlled Database Operation
    if: github.event.inputs.operation == 'performance-schema-status'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment:
      name: production
    concurrency:
      group: iam-production-controlled-database-operation
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run controlled database operation
        uses: appleboy/ssh-action@v1.2.5
        env:
          IAM_DB_OPS_OPERATION: ${{ github.event.inputs.operation }}
          IAM_DB_OPS_ALLOW_DOCKER_CLIENT: "1"
          MYSQL_HOST: ${{ secrets.MYSQL_HOST }}
          MYSQL_PORT: ${{ secrets.MYSQL_PORT || 3306 }}
          MYSQL_USERNAME: ${{ secrets.MYSQL_USERNAME }}
          MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
          MYSQL_DBNAME: ${{ secrets.MYSQL_DBNAME }}
        with:
          host: ${{ vars.SVRB_PUBLIC_HOST || secrets.SVRB_PUBLIC_HOST || vars.SVRB_HOST || vars.SVRA_HOST }}
          username: ${{ vars.SVRB_USERNAME || vars.SVRA_USERNAME }}
          key: ${{ secrets.SVRB_SSH_KEY || secrets.SVRA_SSH_KEY }}
          port: ${{ vars.SVRB_SSH_PORT || vars.SVRA_SSH_PORT || 22 }}
          envs: IAM_DB_OPS_OPERATION,IAM_DB_OPS_ALLOW_DOCKER_CLIENT,MYSQL_HOST,MYSQL_PORT,MYSQL_USERNAME,MYSQL_PASSWORD,MYSQL_DBNAME
          script_path: scripts/dbops/database-operation.sh
          command_timeout: 20m

  db-rolebinding-guard-preflight:
    name: RoleBinding Guard Preflight
    if: github.event.inputs.operation == 'rolebinding-guard-preflight'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: production
    concurrency:
      group: iam-production-controlled-database-operation
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Check active RoleBinding uniqueness
        uses: appleboy/ssh-action@v1.2.5
        env:
          IAM_DB_OPS_OPERATION: rolebinding-guard-preflight
          IAM_DB_OPS_ALLOW_DOCKER_CLIENT: "1"
          MYSQL_HOST: ${{ secrets.MYSQL_HOST }}
          MYSQL_PORT: ${{ secrets.MYSQL_PORT || 3306 }}
          MYSQL_USERNAME: ${{ secrets.MYSQL_USERNAME }}
          MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
          MYSQL_DBNAME: ${{ secrets.MYSQL_DBNAME }}
        with:
          host: ${{ vars.SVRB_PUBLIC_HOST || secrets.SVRB_PUBLIC_HOST || vars.SVRB_HOST || vars.SVRA_HOST }}
          username: ${{ vars.SVRB_USERNAME || vars.SVRA_USERNAME }}
          key: ${{ secrets.SVRB_SSH_KEY || secrets.SVRA_SSH_KEY }}
          port: ${{ vars.SVRB_SSH_PORT || vars.SVRA_SSH_PORT || 22 }}
          envs: IAM_DB_OPS_OPERATION,IAM_DB_OPS_ALLOW_DOCKER_CLIENT,MYSQL_HOST,MYSQL_PORT,MYSQL_USERNAME,MYSQL_PASSWORD,MYSQL_DBNAME
          script_path: scripts/dbops/database-operation.sh

  db-rolebinding-deduplicate-dry-run:
    name: RoleBinding Deduplicate Dry Run
    if: github.event.inputs.operation == 'rolebinding-deduplicate-dry-run'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: production
    concurrency:
      group: iam-production-controlled-database-operation
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Build deterministic candidate report
        uses: appleboy/ssh-action@v1.2.5
        env:
          IAM_DB_OPS_OPERATION: rolebinding-deduplicate-dry-run
          IAM_DB_OPS_ALLOW_DOCKER_CLIENT: "1"
          MYSQL_HOST: ${{ secrets.MYSQL_HOST }}
          MYSQL_PORT: ${{ secrets.MYSQL_PORT || 3306 }}
          MYSQL_USERNAME: ${{ secrets.MYSQL_USERNAME }}
          MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
          MYSQL_DBNAME: ${{ secrets.MYSQL_DBNAME }}
        with:
          host: ${{ vars.SVRB_PUBLIC_HOST || secrets.SVRB_PUBLIC_HOST || vars.SVRB_HOST || vars.SVRA_HOST }}
          username: ${{ vars.SVRB_USERNAME || vars.SVRA_USERNAME }}
          key: ${{ secrets.SVRB_SSH_KEY || secrets.SVRA_SSH_KEY }}
          port: ${{ vars.SVRB_SSH_PORT || vars.SVRA_SSH_PORT || 22 }}
          envs: IAM_DB_OPS_OPERATION,IAM_DB_OPS_ALLOW_DOCKER_CLIENT,MYSQL_HOST,MYSQL_PORT,MYSQL_USERNAME,MYSQL_PASSWORD,MYSQL_DBNAME
          script_path: scripts/dbops/database-operation.sh

  db-rolebinding-deduplicate-apply:
    name: RoleBinding Deduplicate Apply
    if: github.event.inputs.operation == 'rolebinding-deduplicate-apply'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: production
    concurrency:
      group: iam-production-controlled-database-operation
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Soft-delete the exact reviewed candidate set
        uses: appleboy/ssh-action@v1.2.5
        env:
          IAM_DB_OPS_OPERATION: rolebinding-deduplicate-apply
          IAM_DB_OPS_ALLOW_DOCKER_CLIENT: "1"
          IAM_DB_OPS_BACKUP_NAME: ${{ github.event.inputs.backup_name }}
          IAM_DB_OPS_ROLEBINDING_FINGERPRINT: ${{ github.event.inputs.candidate_fingerprint }}
          IAM_DB_OPS_ROLEBINDING_CANDIDATE_COUNT: ${{ github.event.inputs.candidate_count }}
          MYSQL_HOST: ${{ secrets.MYSQL_HOST }}
          MYSQL_PORT: ${{ secrets.MYSQL_PORT || 3306 }}
          MYSQL_USERNAME: ${{ secrets.MYSQL_USERNAME }}
          MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
          MYSQL_DBNAME: ${{ secrets.MYSQL_DBNAME }}
        with:
          host: ${{ vars.SVRB_PUBLIC_HOST || secrets.SVRB_PUBLIC_HOST || vars.SVRB_HOST || vars.SVRA_HOST }}
          username: ${{ vars.SVRB_USERNAME || vars.SVRA_USERNAME }}
          key: ${{ secrets.SVRB_SSH_KEY || secrets.SVRA_SSH_KEY }}
          port: ${{ vars.SVRB_SSH_PORT || vars.SVRA_SSH_PORT || 22 }}
          envs: IAM_DB_OPS_OPERATION,IAM_DB_OPS_ALLOW_DOCKER_CLIENT,IAM_DB_OPS_BACKUP_NAME,IAM_DB_OPS_ROLEBINDING_FINGERPRINT,IAM_DB_OPS_ROLEBINDING_CANDIDATE_COUNT,MYSQL_HOST,MYSQL_PORT,MYSQL_USERNAME,MYSQL_PASSWORD,MYSQL_DBNAME
          script_path: scripts/dbops/database-operation.sh
