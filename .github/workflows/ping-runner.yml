name: Deprecated - Ping Runner

# Deprecated: use "Production Health Check" (server-check.yml) instead.
# This workflow is intentionally manual-only so the old scheduled ping does not
# duplicate the production health check or probe the retired host-port layout.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    name: Deprecated workflow notice
    runs-on: ubuntu-latest
    steps:
      - name: Show replacement
        run: |
          echo "ping-runner.yml is deprecated."
          echo "Use the Production Health Check workflow instead."
