# 配置加载与运行模式

## 30 秒结论

运行配置决定 IAM 如何连接 MySQL、Redis、JWT key、IDP、Suggest 和后台任务。文档只能解释配置语义，具体字段以配置结构和样例为准。

## 事实源

- `../../internal/apiserver/config`
- `../../configs`
- `../../configs/env`

## 写作规则

- 不在文档中复制大段配置样例。
- 配置字段变化时优先更新配置样例和代码，再更新文档说明。
- 规划中的配置必须标记为 `规划改造`。
