# Identity 领域模型图

> 状态：待补证据 · 骨架初稿，待与代码/契约核对。

```mermaid
classDiagram
    class User {
        +ID
        +Name
        +Phone
        +Email
        +Status
    }
    class Profile {
        +ID
        +Name
        +Gender
        +Birthday
        +IDCard
    }
    class ProfileLink {
        +ID
        +UserID
        +ProfileID
        +Type
        +Relation
        +RevokedAt
    }
    User "1" --> "0..*" ProfileLink
    Profile "1" --> "0..*" ProfileLink
```
