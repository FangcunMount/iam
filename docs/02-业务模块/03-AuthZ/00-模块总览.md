# AuthZ 模块总览

## 模块定位

AuthZ 建模访问权，负责授权写入、版本传播、权限检查和运行时策略匹配。

AuthZ 不负责：

- 登录认证。
- Token 签发。
- User/Profile 写模型。
- ProfileLink 关系治理。

## 核心模型

```text
Subject
  -> RoleBinding
  -> Role
  -> Permission
  -> Resource / Action / Scope
  -> AuthorizationDecision
```
