# 契约测试

> 状态：待补证据 · 骨架初稿，待与代码/契约核对。

## REST

REST 契约以 OpenAPI 和运行时路由为准。

```bash
make api-validate
```

## gRPC

gRPC 契约以 proto 和运行时注册为准。

```bash
make proto-gen
go test ./internal/apiserver/transport/grpc
```

## 规则

- 接口字段不要只改文档。
- 传输层 DTO 不进入 domain。
- 契约变更必须同步接入文档。
